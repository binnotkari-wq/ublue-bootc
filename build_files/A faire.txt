Erreur spécifique à la customisation :


BUILD GITHUB
Lint warning: nonempty-run-tmp: Found content in runtime-only directories (/run, /tmp):
  /run/dnf

Lint warning: var-tmpfiles: Found non-directory/non-symlink files in /var:
  var/lib/dnf/system-repo.lock

---> des solutions existent peut etre chez universal blue


PROBLEMES AU BOOT ISO
Au démarrage de l'iso (uniquement l'iso, une fois le système installé, ces erreurs n'existent pas)
dracut-cmdline[323]: Warning: .buildstamp missing
dracut-pre-udev[707]: modprobe: ERROR: could not insert 'edd' : No such device



PROBLEMES OS - TOUS EN UPSTREAM FEDORA (Erreurs constatées également sur un fedora 44 d'origine)
secure boot pas activé (la faute à la VM ?)

Services failed (systemctl --failed) :
● mcelog.service
● systemd-remount-fs.service 

toutes les erreurs systemd-remount-fs exit status 32 (à ignoer, c'est la nature meme de la racine rpm ostree)
toutes les erreurs systemd-tmpfiles sur /home /srv /root (à ignoer, c'est la nature meme de la racine rpm ostree)
erreur gdm-password][2333]: gkr-pam
Les erreurs systemd-remount-fs sont aussi signalées pendant le build sur github (sans echec)

Pour info, Fedora fait aussi d'autres erreurs absentes de l'image bootc perso :
- systemd-tmpfiles (Failed to resolve group 'tss')
- dbus-broker-launch: Ignoring duplicate name (conséquence de la préinstallation des flatpak qui cherchent dbus oas encore actif)
- fedora systemd: Failed to start app-gnome-vmware



ETAT DE VALIDATION

.
├── artifacthub-repo.yml
├── build_files
│   ├── A faire.txt                                             # NC
│   ├── build.sh                                                # OK
│   ├── softwares.sh                                            # OK
│   ├── trimming.sh                                             # OK
│   └── tweaks.sh                                               # OK
├── Containerfile                                               # OK
├── cosign.key                                                  # NC
├── cosign.pub                                                  # NC
├── disk_config
│   ├── disk.toml                                               # OK
│   ├── iso-gnome.toml                                          # OK
│   └── iso-kde.toml                                            # NC
├── image-template.env
├── Justfile                                                    # NC
├── LICENSE                                                     # NC
├── README.md                                                   # NC
└── system_files         ######################################## OK
    ├── etc
    │   ├── dconf
    │   │   ├── db
    │   │   │   └── local.d
    │   │   │       └── 00-defaults                             # NOK syntaxe dash to panel position en haut
    │   │   └── profile
    │   │       └── user                                        # OK
    │   ├── firefox
    │   │   └── policies
    │   │       └── policies.json                               # OK
    │   ├── profile.d
    │   │   └── 10-environment.sh                               # NOK intégrer alias pour system-update.sh 
    │   ├── scripts
    │   │   ├── bootc-welcome.sh                                # OK
    │   │   └── system-update.sh                                # OK
    │   └── skel
    │       └── Modèles
    │           ├── Fichier Markdown.md                         # OK
    │           ├── Fichier texte.txt                           # OK
    │           └── Script.sh                                   # OK
    └── usr



image :
A modifier, en date du 06/08/2026
Erreur de syntaxe pour la position de dash to panel, corrigé, en attente rebuild pour tester
Assigne un alias pour le script d'update system



intégrer steam (si diff de perfs en faveur des rpm, et si il est impossible d'avoir une session gamescope en flatpak)






BUILD LOCAL
===================================
infos générales

Ce que fait réellement bootc-image-builder sous le capot

Pour transformer une image de conteneur en ISO d'installation autonome, l'outil exécute plusieurs étapes gourmandes en processeur et en entrées/sorties disque :

    Extraction et conversion du système de fichiers
    Il ne se contente pas de copier le conteneur. Il extrait toutes les couches (layers), reconstitue l'arborescence complète et formate une vraie partition disque virtuelle (dans votre cas, en btrfs).

    Génération d'un initramfs dédié (Dracut)
    Il doit compiler un noyau et un système de démarrage temporaire (initramfs) capable de détecter le matériel de n'importe quel PC au boot de l'ISO. C'est l'étape qui génère souvent les avertissements grub2-probe que vous avez vus passer.

    Création de l'image OSTree / Native Container
    Il prépare la structure ostree ou le magasin de conteneurs local qui sera directement copié sur le disque dur de la machine cible lors de l'installation.

    Compression extrême (La partie la plus lente)
    L'ISO utilise un système de fichiers compressé en lecture seule (SquashFS). La compression de plusieurs gigaoctets de données pour faire tenir l'OS et l'installeur Anaconda sur une ISO prend un temps considérable et sollicite énormément le processeur.

Pourquoi c'est beaucoup plus long que podman build ?

    podman build ne fait qu'ajouter des couches légères et mettre à jour du texte/binaires dans un cache local.

    bootc-image-builder fabrique une véritable image disque complète, initialise une table de partition (MBR/GPT), installe et configure GRUB, et compresse l'ensemble du système de fichiers.




=======================================
# build offline du bootc

# Se placer dans le repo
cd /home/benoit/Git/ublue-bootc

# construire l'image :
sudo podman build -t ublue-bootc:latest .
=======================================


=======================================
# build offline de l'image
# construire le container :
skopeo copy containers-storage:localhost/ublue-bootc:latest \
  oci:/cargo/local_cache/bootc-images/ublue-bootc_$(date +%Y%m%d-%H%M%S).oci
  
# intégration sur l'hôte :
  sudo bootc switch --transport oci /cargo/local_cache/bootc-images/ublue-bootc_"versionsouhaitée".oci
=======================================




=======================================
# build offline de l'iso
Tout simlement avec le script fait par Gemini

========================================


.
├── artifacthub-repo.yml
├── build_files
│   ├── A faire.txt                                             # NC
│   ├── build.sh                                                # OK
│   ├── softwares.sh                                            # OK
│   ├── trimming.sh                                             # OK
│   └── tweaks.sh                                               # OK
├── Containerfile                                               # OK
├── cosign.key                                                  # NC
├── cosign.pub                                                  # NC
├── disk_config
│   ├── disk.toml                                               # OK
│   ├── iso-gnome.toml                                          # OK
│   └── iso-kde.toml                                            # NC
├── image-template.env
├── Justfile                                                    # NC
├── LICENSE                                                     # NC
├── README.md                                                   # NC
└── system_files         ######################################## OK
    ├── etc
    │   ├── dconf
    │   │   ├── db
    │   │   │   └── local.d
    │   │   │       └── 00-defaults                             # NOK syntaxe dash to panel position en haut
    │   │   └── profile
    │   │       └── user                                        # OK
    │   ├── firefox
    │   │   └── policies
    │   │       └── policies.json                               # OK
    │   ├── profile.d
    │   │   └── 10-environment.sh                               # OK
    │   ├── scripts
    │   │   ├── bootc-welcome.sh                                # OK
    │   │   └── system-update.sh 
    │   └── skel
    │       └── Modèles
    │           ├── Fichier Markdown.md                         # OK
    │           ├── Fichier texte.txt                           # OK
    │           └── Script.sh                                   # OK
    └── usr
























pour flathub
https://github.com/ublue-os/main/blob/main/sys_files/usr/lib/systemd/system/flatpak-add-flathub-repos.service

Les preferences ne s'appliquent pas lorsqu'elles fonc partie d'un upgrade, et que l'utilisateur a déjà modifié des réglages sur ces preferences.


  A installer dans brew
    htop
    kitty                       # console accelerée GPU, esthétique
    dust                        # analyse graphique de l'espace disque
    duf                         # Visualisation rapide de l'espace disque
    # stow                      # Gestion des dotfiles personnels (inutile lorsqu'on déclare les préférences en .nix)
    # cliphist                    # Visualisation de l'historique du presse-papier. Mais ne sert pas trop à grand chose.
    # atuin                     # analyse de l'historique bash. Mais par rappor à la confidentialité ....non (synchro de l'historique en ligne, etc...)
    ffmpeg
    groff
    imagemagick
    pandoc
    nerd-fonts.jetbrains-mono
    # superfile                 # gestionnaire de fichiers esthétique
    # ranger                    # gestionnaire de fichiers esthétique
    lynx                        # browser texte basique
    w3m                         # navigateur internet
    zellij                      # multiplexeur de terminal
    yazi                       # gestionnaire de fichiers TUI modulaire
    yaziPlugins.mount
    yaziPlugins.chmod
    yaziPlugins.compress
    yaziPlugins.full-border
    yaziPlugins.recycle-bin
    htop                        # Le classique immanquable
    mplayer                     # nécessaire à pyradio
    pyradio                     # radio web
    newsboat                    # lecteur RSS
    television                  # interface TUI à fd
    # tmux                      # multiplexeur de terminal
    md-tui                      # visualisateur Markdown. Le plus agréable. Permet d'editer, de rechercher...mais parfois n'interprète pas bien de md.
    musikcube                   # Lecteur mp3 esthétique
    # aerc                      # client mail
    gophertube                  # chercher, regarder et télécharger des vidéos depuis youtube
    browsh                      # browser texte esthétique
    # docker : pas justifié quand on a deja podman et en plus distrobox
    #just                                                # Exécuteur de commandes de projet. Too-much, pas de cas d'usage réel
